[project]
name = "math-game"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sahana", email = "sahana2008lol@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
]

[project.scripts]
math-game = "math_game:main"

[build-system]
requires = ["uv_build>=0.8.2,<0.9.0"]
build-backend = "uv_build"
